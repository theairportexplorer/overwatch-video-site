pipeline {
    // agent { docker { image 'python:3.7.4-alpine3.10' } }
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "hello, world!"'
                // sh 'python --version'
                sh 'pwd'
                sh 'ls'
            }
        }
    }
}
