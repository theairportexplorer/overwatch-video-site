pipeline {
    agent { docker { image 'python:3.7.4-alpine3.10' } }
    stages {
        stage('Test') {
            steps {
                sh 'echo "hello, world!"'
                sh 'python --version'
                sh 'pwd'
                sh 'ls'
            }
        }
    }
}